#include<stdio.h>
int main(){
    char ch;
    char s[1];
    char sen[100];
    scanf("%c\n",&ch);
    printf("%c\n",ch);
    scanf("%s\n",s);
    printf("%s\n",s);
   fgets(sen, sizeof(sen), stdin);
    printf("%s",sen);
    return 0;
    
}
